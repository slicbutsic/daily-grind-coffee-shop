<div class="mt-5 container mx-auto px-4 py-8 md:py-12 lg:py-16">
  <div class="flex flex-col md:flex-row items-center md:items-start space-y-8 md:space-y-0 md:space-x-8 lg:space-x-12">
    <%# partial product image %>
    <%= render 'product_show_image', product: @product %>
    <%# partial product details %>
    <%= render partial: 'product_details', locals: { product: @product } %>
  </div>
</div>
